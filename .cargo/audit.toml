# cargo-audit configuration
# https://rustsec.org/

[advisories]
ignore = [
    # instant crate unmaintained — transitive dep via nostr; no upstream fix
    "RUSTSEC-2024-0384",
    # proc-macro-error (build-time) via glib-macros/gtk3-macros 0.18; remove once gtk-rs stack is bumped
    "RUSTSEC-2024-0370",
]
